import Styled from "styled-components";
import { Row as R, Col as C } from "styled-bootstrap-grid";

export const Row = Styled(R)`
    margin: 0 !important;
`
export const Col = Styled(C)`
    margin: 0 !important;
`